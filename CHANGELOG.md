# Changelog

## 1.0.0-submission

- FastAPI API와 최소 HTML UI를 추가했습니다.
- `/health`, `/metrics`, `/api/scenarios`, `/api/policies`, `/api/run-scenario`, `/api/reports/latest` endpoint를 제공합니다.
- Dockerfile, 실행 스크립트, API smoke script, GitHub Actions CI, Dependabot 설정을 추가했습니다.
- OSS 제출 문서, runbook, model card, retrospective, submission checklist를 정리했습니다.

## MVP-0 Research Harness

- EntityToken, RiskToken, RelationToken, EventToken, UncertaintyToken, EvidenceToken, AffordanceToken을 구현했습니다.
- Object List, Flat Feature Table, Scene Graph, RAST, Event-aware RAST, Uncertainty-aware RAST, Affordance-aware RAST 비교 harness를 구축했습니다.
- sampled extended evaluation, seed stability, sample-size convergence, replay artifact, result/technical report를 생성했습니다.
